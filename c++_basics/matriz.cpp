#include<bits/stdc++.h>

using namespace std;

#define ll long long

int32_t main(){
    ll n;
    cin >> n;
    vector<ll> v(n,1);
    // cout << v.size() << endl;
    vector<vector<ll>> mat(n, vector<ll>(n,0));
    // vector mat(n, vector<int>(n,0));
    // vector mat(n, vector(n,0ll));
    // vector mat(n, vector<int>(n,0));
    for(int i = 0; i < n; i++){
        for(int i = 0; i < n; i++){

        }
    }
    return 0;
}